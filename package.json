{
  "name": "parry-feed",
  "version": "2.0.0",
  "private": true,
  "description": "Threat-feed pipeline for the ScamShield (formerly ScamShield) extension.",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.js",
    "dry-run": "node build.js --dry-run",
    "test": "node --test"
  }
}
